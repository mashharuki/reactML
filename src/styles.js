/**
 * スタイル用のファイル
 */
const styles = {
  app: {
    textAlign: 'center'
  },
  canvas: {
    width: 280,
    height: 280,
    border: '1px solid silver'
  },
  predict: {
    fontSize: 24,
    backgroundColor: 'yellow'
  }
}

// 外部に公開する
export default styles;
